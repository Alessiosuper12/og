<Window x:Class="ProjectZein.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectZein"
        mc:Ignorable="d"
        Title="Project Zein - Fortnite Launcher" Height="250" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Fortnite Executable Path:" Grid.Row="0"/>

        <Grid Grid.Row="1" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathTextBox" Grid.Column="0" Margin="0,0,5,0" VerticalContentAlignment="Center"/>
            <Button x:Name="BrowseButton" Content="Browse..." Grid.Column="1" Padding="10,2" Click="BrowseButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
             <Button x:Name="LaunchButton" Content="Launch &amp; Inject" Padding="20,10" FontSize="16" Click="LaunchButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
